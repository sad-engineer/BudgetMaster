<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BudgetMaster" parent="Base.Theme.BudgetMaster" />

    <!-- Стили для кнопок валют -->
    <style name="CurrencyButton.Add" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/circle_button_background</item>
        <item name="android:colorAccent">@color/add_button_background</item>
    </style>

    <style name="CurrencyButton.Delete" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/circle_button_background</item>
        <item name="android:colorAccent">@color/delete_button_background</item>
    </style>
</resources>